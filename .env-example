# Configuração local do nó (ver internal/cfg e cmd/node/main.go).
# Valores em branco ou ausentes usam o default do cleanenv no código.

MEMDB_MAX_ENTRIES=2000
CHECKPOINT_INTERVAL=1m
FS_DEFER_WRITES=false
FS_FLUSH_INTERVAL=1m
CHECKPOINT_FILE_NAME="checkpoint.cbor"
